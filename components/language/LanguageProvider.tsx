/**
 * @deprecated Use `useTranslations()` from `next-intl` and `@/components/i18n/LanguageSwitcher`.
 * Kept temporarily for reference during migration.
 */
export {
  useLocale,
  useTranslations as useLanguage,
} from 'next-intl'
